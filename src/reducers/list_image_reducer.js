import * as CONSTANTS from "../constants";

const initialState = [
  CONSTANTS.image1,
  CONSTANTS.image2,
  CONSTANTS.image3,
  CONSTANTS.image4
];

export const ListImage = (state = initialState, action) => {
  return state;
};
